import { mutation } from './_generated/server'

// export default mutation(async ({ db }, id: GenericId) => {
//   const assignment = { class_name, assignment_name, due_date, source_url }
//   await db.replace('assignment', assignment)
// })